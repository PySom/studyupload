namespace StudyMATEUpload.Enums
{
    public enum StudyType : byte { StudyMate, Main }
    public enum StudyLevel : byte { None, Nursery, Primary, Junior, Senior, Tertiary }
    public enum Level : byte { One, Two, Three, Four, Five, Six, Seven, Eight, Nine, Ten }
    public enum Sex : byte { None, Female, Male }
    public enum Mode : byte { Learn, Time, Free }
    public enum Role : byte { None, Student, Teacher, School, Admin }
}
